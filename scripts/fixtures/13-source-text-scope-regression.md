<!-- fixture: regression test for the source_text-widening bug (see
     facts-source-text-regression.md). No row in that registry is about the
     programs count below -- that figure only appears because it rides along
     inside a source_text quote shared by two unrelated rows. This claim must
     ERROR as zero-source, not trace cleanly. The "directed programs" line is the
     control: a figure nothing in the registry names at all, included so this
     test can't pass vacuously (both a real fix and a totally broken matcher
     would ERROR on everything). The final line proves the registry rows are
     actually live -- both g1 and g2 must still trace cleanly through their own
     value fields. -->

# Tailored Resume - Riley Chen

## Experience

- Managed 3 education programs serving families across the region.
- Directed 9 education programs at a separate nonprofit.
- Served 1,200 students annually across 4 sites.
