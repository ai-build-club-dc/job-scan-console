<!-- fixture: added for the credential-regex regression that motivated this test
     suite. The initialism meaning Large Language Model must NEVER fire the
     credential check, in any of its ordinary AI-work phrasings below (bare,
     hyphenated-modifier, "an ... agent"). The same three letters, written in
     period form, or written bare but immediately followed by a preposition (how
     the law degree is actually written on a résumé), must still fire it -- that is
     the genuine positive this pattern exists to catch, and must not become
     collateral damage from the fix. See the material lines below for the exact
     forms under test. -->

# LLM Regression Probe

- Prototyped an LLM-based summarizer over support tickets.
- Worked closely with an LLM agent to review pull requests.
- Ran a bare LLM evaluation harness against three prompts.
- LL.M. in Taxation, State University.
- LLM in Taxation, State University.
- LLM of Comparative Law, State University.
