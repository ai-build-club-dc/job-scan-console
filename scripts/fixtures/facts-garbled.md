<!-- fixture registry: not a markdown table at all -- probes parse_registry's
     "no header row found anywhere" path. Must not crash; must report the registry
     as empty with an explanatory note, and every material linted against it should
     report every numeric/date/credential claim as zero-source. -->
This is not a table at all, just prose.
Some | random | pipes | here but no header row.
